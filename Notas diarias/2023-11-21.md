

```
paste(str_remove_all(colnames(snvs_nDx_top), "\\.\\w")
```

```r
oncoPrint(snvs_nDx_top,
                alter_fun = inc.oncoprintR::alter_fun_default_single,
                col = inc.oncoprintR::cols_barplot,
                show_column_names = TRUE,
                column_order = colnames(snvs_nDx_top),
                row_order = rownames(snvs_nDx_top),
                right_annotation = NULL,
                remove_empty_rows = FALSE,
                heatmap_legend_param = lgd,
                column_split = str_remove_all(colnames(snvs_nDx_top), "\\.\\w"),
                column_labels = str_remove_all(colnames(snvs_nDx_top), "\\.\\w"),
                column_gap = unit(3, "mm"),
                column_title = NULL)
```