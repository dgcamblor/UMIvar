Created SSH key pair:

```
ssh-keygen -t ed25519 -C "danielgcamblor@outlook.es"
```

Copied public key:

```
cat ~/.ssh/id_ed25519.pub
```

And pasted it into GitLab. 

Connecting to the Git:

```
ssh -T git@vgit0srv.bioinfo.incliva
```