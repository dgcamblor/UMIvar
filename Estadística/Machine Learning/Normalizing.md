Ensures a variable can be described as a normal distribution. It is useful for techniques that assume normally distributed data, such as [[LInear DIscriminant Analysis (LDA)]] OR [[Naive Bayes]] (Gaussian).

Normalization can be performed with the [[Transformación de Box-Cox]].