
## git pull
## git push

Upload changes upstream, to the remote repository. The counterpart to [[Git#git pull]].

```
git push <remote_repository>
```

- `--tags`: Also upload tags.

Changes can be pushed to the origin.

```
git push origin
```